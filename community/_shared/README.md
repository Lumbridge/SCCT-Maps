# Loose supporting files

These original pack files have no confirmed per-map association in the import tables. Copy `Packages` into the game alongside the selected map to retain these supporting files. The original paths and contents are preserved; none were taken from the installed game.
